module.exports = {
    info: {
        // API informations (required)
        title: 'TO-DO LIST API', // Title (required)
        version: '3.0.0', // Version (required)
        description: 'API for auth and to-do list interactions', // Description (optional)
    }
};